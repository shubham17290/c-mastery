//  inbuilt function in the string -
//  1. strlen() - to find the length of the string

#include <stdio.h>
#include <string.h>
int main() {
    int i;
    char *p = "shubham";
    i = strlen(p);
    printf("%d", i);

    return 0;
}
