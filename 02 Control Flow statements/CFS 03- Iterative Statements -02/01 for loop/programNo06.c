#include <stdio.h>

int main()
{
    int n;
    int count = 0;
    //  note the scope of the for loop is to first semicolon , if you are note initialized the brackets
    for (int i = 1; i <= n; i= i*2)
    {
//  code = execute will be - > 1+ logbase2(n) times
    }
    return 0;
}