//  real example of post-increment
#include <stdio.h>

int main() {
    int a = 5;

    printf("%d \n", a++); //  a is used first --> 5
    // increment in  a
    printf(" Now the value of the a is = %d\n", a); // now a = 6

    return 0;
}
