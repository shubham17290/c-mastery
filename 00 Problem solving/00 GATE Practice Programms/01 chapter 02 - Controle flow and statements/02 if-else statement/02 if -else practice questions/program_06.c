#include <stdio.h>

int main() {
#include <stdio.h>

    int main() {
        int a, b, c, d, e;
        if (ia > b && c < d || e) {
            printf("that you for putting an input value \n");
        }
    }

    return 0;
}
