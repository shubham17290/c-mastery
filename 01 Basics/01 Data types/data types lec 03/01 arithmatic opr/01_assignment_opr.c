#include <stdio.h>

int main()
{

    int a, b, c;
    a = 10;
    b = 20;
    c = a + b; // this is expression 
    /*
       the standard format of assignment operator(syntax): L.value = R.value
         
          1.L.value: only can have variable
          2.R.value : this one can have : (the literal value , variable and can have expression as well )


    */

    printf("the sum of the %d and %d is %d", a,b,c);
    

    return 0;
}


