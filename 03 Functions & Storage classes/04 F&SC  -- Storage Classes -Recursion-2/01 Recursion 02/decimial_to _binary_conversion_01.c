#include <stdio.h>
void f(int n) {
    // if () { // base case --> works for the small numbers

    // } else { // large or main case --> it include's the recursion call  and have the recursion
             // function that helps in terms calling the function again and again
    // }
}
void main() {}
