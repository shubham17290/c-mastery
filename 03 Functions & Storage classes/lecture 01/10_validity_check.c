#include <stdio.h>
int mul(int, int);
void main() {
    int a = 10, b = 20;
    mul(a, b);
}
int mul(int x, int y) {
    int result;
    result = x * y;
}
//  no error will be there there is no print , but there is function call 
